name: Mutation Testing

on:
  push:
    branches: [ tache3-integration ]
  pull_request:
    branches: [ main ]

jobs:
  mutation-test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
    
    - name: Build entire project
      run: mvn clean install -DskipTests
    
    - name: Run Mutation Tests with PIT
      run: mvn org.pitest:pitest-maven:mutationCoverage
      working-directory: ./core

    - name: Check Mutation Score
      id: mutation-check
      if: always()
      continue-on-error: true
      run: python3 .github/scripts/check-mutation-score.py

    - name: Rickroll on Failure
      if: always()
      uses: ./.github/actions/rickroll
      with:
        test-result: ${{ steps.mutation-check.outcome }}
    
    - name: Fail if mutation score dropped
      if: steps.mutation-check.outcome == 'failure'
      run: exit 1
    
    - name: Upload Mutation Report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: pit-reports
        path: core/target/pit-reports/
